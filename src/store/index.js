import Vue from 'vue'
import Vuex from 'vuex'

import home_store from './home'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    home_store,
  }
})
